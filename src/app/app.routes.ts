import { Routes } from '@angular/router';
import { CurrencyCardWrapperComponent } from './components/currency-card-wrapper/currency-card-wrapper.component';

export const routes: Routes = [
    { path: '', component: CurrencyCardWrapperComponent }
];
